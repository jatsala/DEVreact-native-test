{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"], // If you're only using the "src" folder use: "src/*"
      "@components/*": ["src/components/*"]
    }
  }
}
